archivesBaseName = 'article-service'

jar.enabled = true
bootJar.enabled = false

dependencies {
    compile project(':article:article-repository')
    compile project(':security:security-service')
    compile project(':core')
}
