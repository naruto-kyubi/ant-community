archivesBaseName = 'user-reposistory'

jar.enabled = true
bootJar.enabled = false

dependencies {
    compile project(':user:user-domain')
    compile project(':core')
    implementation 'org.springframework.boot:spring-boot-starter-data-jpa'
}
